/*
  program (4) Write a program to calculate the area of
circle
Solution (2) user Define valives
*/
#include <stdio.h>
int main()
{
float radius, area;
Printf("\n Enter the radius of circle:");
scanf (" %f", & radius);
аreа = (3.14* radius* radius);
Printf ("\n Area of circle=%f", area);
return 0;
}
